import React from 'react';
// import { LucideCalculator } from 'lucide-react';

const ProjectActionCards = ({ onShowCalculator }) => {
    return null; // Oculto por solicitud del usuario
    /* return (
        <div className="bg-white dark:bg-slate-900 rounded-[32px] p-6 border border-slate-100 dark:border-slate-800 shadow-sm">
            <p className="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-4">Acciones Rápidas</p>
            <button onClick={onShowCalculator} className="w-full py-4 bg-slate-50 dark:bg-slate-800 text-slate-900 dark:text-white hover:bg-slate-100 dark:hover:bg-slate-700 rounded-2xl flex items-center justify-center gap-3 transition-all border border-slate-100 dark:border-slate-700">
                <LucideCalculator size={18} className="text-primary" />
                <span className="text-[10px] font-black uppercase tracking-widest">Calculadora Manual</span>
            </button>
        </div>
    ); */
};

export default ProjectActionCards;
